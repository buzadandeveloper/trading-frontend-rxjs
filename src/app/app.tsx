// import { klineWs, miniTickerService } from '../services';
// import { useObservableState } from 'observable-hooks';

function App() {
  // const marketData = useObservableState(miniTickerService.allMiniTickers$, null);
  //
  // console.log(marketData);

  // const getminiTicker = useObservableState(miniTickerService.getAllMiniTickers$, null);
  //
  // console.log(getminiTicker);

  return <div></div>;
}

export default App;
